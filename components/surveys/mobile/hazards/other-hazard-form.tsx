'use client'

// Legacy component — hazards are now captured per-area in the hazard capture screen.
// Kept as stub for backward compatibility with index.ts export.
export function OtherHazardForm() {
  return null
}
